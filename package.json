{
  "name": "earthquake-visualizer",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://1456vk.github.io/earthquake-visualizer",
  "dependencies": {
    "axios": "^1.6.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-leaflet": "^4.2.1",
    "leaflet": "^1.9.4",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "update-deploy": "git add . && git commit -m \"update project\" && git push origin main && npm run deploy",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
